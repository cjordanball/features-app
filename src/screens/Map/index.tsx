import { Text, View } from 'react-native';
import { styles } from './mapStyle';

const Map = () => {
	return (
		<View style={styles.container}>
			<Text>Map Page</Text>
		</View>
	);
};

export default Map;
